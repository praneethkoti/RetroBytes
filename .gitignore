# Build artifacts
retrobytes.exe
*.exe
/bin/

# Go build/test artifacts and coverage
go-cache/
*.test
*.out

# Runtime-generated data and logs (auto-created on run)
retrobytes.db
*.db
retrobytes.log
*.log

# Secrets / local environment
.env
.env.*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Intentionally-untracked sensitive documents (kept locally, not published).
# Anchored to exact paths so the rest of documents/ stays tracked.
# A third party's review work product (contains their name and email):
/documents/RetroBytes - Secure Code Review Report by reviewer.docx
# Instructor course material, not for a public repo:
/documents/ENPM680 - Intro to Secure Software Engineering Project.pdf
